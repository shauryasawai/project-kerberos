import { styled } from 'styled-components';

export const Container = styled.div`
  max-height: 400px;
`;

export const Map = styled.img`
  border-radius: 4px;
  max-width: 100%;
  max-height: 100%;
  object-fit: contain;
`;
